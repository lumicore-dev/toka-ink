import build::{Executable, run_build}

fn main() -> i32 {
    auto app = Executable(
        name = "app",
        version = "0.1.1",
        src = "src/main.tk",
        deps = string::from("")
    )
    return run_build(app)
}
